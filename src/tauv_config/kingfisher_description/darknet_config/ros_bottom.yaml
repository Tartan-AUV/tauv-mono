subscribers:

  camera_reading:
    topic: /zedm_B/zed_node_B/left/image_rect_color
    queue_size: 1

actions:

  camera_reading:
    name: /darknet_ros_bottom/check_for_objects

publishers:

  object_detector:
    topic: /darknet_ros_bottom/found_object
    queue_size: 1
    latch: false

  bounding_boxes:
    topic: /darknet_ros_bottom/bounding_boxes
    queue_size: 10
    latch: false

  detection_image:
    topic: /darknet_ros_bottom/detection_image
    queue_size: 10
    latch: true

image_view:

  enable_opencv: true
  wait_key_delay: 1
  enable_console_output: false
