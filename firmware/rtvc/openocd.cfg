source [find interface/stlink.cfg]

transport select hla_swd

source [find target/stm32f7x.cfg]

reset_config none

init
reset halt

# Optional: Force software reset via SYSRESETREQ (AIRCR)
# cortex_m reset_config sysresetreq
