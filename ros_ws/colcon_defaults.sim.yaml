build:
  packages-skip:
    - tauv_vehicle
  symlink-install: true
  merge-install: true
  cmake-args:
    - "-DCMAKE_EXPORT_COMPILE_COMMANDS=ON"
    - "-G Ninja"
    - "-DCMAKE_BUILD_TYPE=Debug"
  event-handlers:
    - "console_cohesion+"
